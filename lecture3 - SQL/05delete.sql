-- deleting a row from a table

DELETE FROM flights WHERE destination = 'Tokyo';

-- deletion cannot be undone, but database backups can be made

-- id of a deleted row is not used for another new row